apiVersion: v1 
kind: Service 
metadata:
  name: sonarqube
  namespace: sonarqube
  labels:  
    app: sonarqube  
spec:  
  type: LoadBalancer
  selector:  
    app: sonarqube
  ports:  
    - protocol: TCP 
      port: 80  
      targetPort: 9000  